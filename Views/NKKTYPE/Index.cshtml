@model IEnumerable<PMS.NKKTYPE>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NKKCODE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NKKITEM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NKKDESC)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NKKCODE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NKKITEM)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NKKDESC)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PK }) |
            @Html.ActionLink("Details", "Details", new { id=item.PK }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PK })
        </td>
    </tr>
}

</table>
