@model PMS.CURRMAST

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>CURRMAST</legend>

        @Html.HiddenFor(model => model.PK)

        <div class="editor-label">
            @Html.LabelFor(model => model.CURCODE)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.CURCODE)
            @Html.ValidationMessageFor(model => model.CURCODE)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.CURNAME)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.CURNAME)
            @Html.ValidationMessageFor(model => model.CURNAME)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.CURNUM)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.CURNUM)
            @Html.ValidationMessageFor(model => model.CURNUM)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
