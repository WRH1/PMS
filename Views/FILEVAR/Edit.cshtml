@model PMS.FILEVAR

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>FILEVAR</legend>

        @Html.HiddenFor(model => model.PK)

        <div class="editor-label">
            @Html.LabelFor(model => model.DBF)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DBF)
            @Html.ValidationMessageFor(model => model.DBF)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.VARNAME)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.VARNAME)
            @Html.ValidationMessageFor(model => model.VARNAME)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
